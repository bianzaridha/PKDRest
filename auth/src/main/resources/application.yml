spring:
    application:
        name: auth-service
        
server:
    port: 8000
    
eureka:
    client:
        registerWithEureka: true
        fetchRegistry: true
        serviceUrl:
            defaultZone: http://localhost:8070/eureka
    instance:
        hostnmae: localhost
